# Provenance pin of the claude-code-framework grafted into this project.
# It serves the upgrade procedure of the framework's SETUP.md, section
# "Upgrading the framework on an already-grafted project" (Step 0 and Step 6),
# as the certain baseline of the 3-way merge.
# Flat `key: value` lines, no parser. It lives OUTSIDE .claude/memory/ on purpose,
# so the upgrade's "empty diff on memory/" invariant stays intact.
version: 1.2.0
commit: 7d6a9f7
grafted: n/d (retrofit 2026-07-19)
