---
type: session
date: 2026-07-18
branch: docs/readme-v1.3.0
tags: [session, readme, docs, release-v1.3.0]
---
# README allineato alla realtà di v1.3.0 (pre-release)

## Contesto
Decisione utente 2026-07-18: **si rilascia v1.3.0 ORA**. Micro-sequenza in due
task: (1) README ↔ realtà post-M2 (questo), (2) release v1.3.0 (VERSION +
CHANGELOG + blocco /integrate). Framework a v0.5.1: diff docs/03+04 rispetto a
come lavoravamo = solo 2 precisazioni (baseline gitleaks storia preesistente;
razionale `git describe --tags` in /integrate), nessun cambio di regole.

## Fatto (commit `9ceb69b`, 7 righe sostituite)
- Callout `las` riscritto: un agente esegue ESATTAMENTE la selezione salvata
  (plist → `brew_manager.sh <moduli> --yes`), validata a ogni path di scrittura
  (install/modify/re-register; skip sul restore bk). Era il residuo "not yet
  honoured" pre-M2.
- Invariante fail-closed documentata (nota "Non-interactive runs"): senza
  `--yes` una run non-TTY NEGA ogni prompt — ispeziona, non modifica mai.
- bk: aggiunta la validazione del restore agenti (entry invalida → skip con
  warning, mai fallback-`go`).
- **2 claim corretti perché la verifica dal vivo li ha smentiti** (onestà):
  1. "unknown module token → non-zero exit": FALSO end-to-end. Il figlio esce 2
     ma il parent perde l'exit nella strip ANSI dopo `script(1)`
     (`brew_manager.sh:245-250`: `exit $?` riporta l'esito di `mv`). Claim
     rimosso ("prints an error and runs nothing"); resta vero per i FLAG ignoti
     (validati pre-recording, misurato exit 2). Registrato in Attenzione #4b.
  2. "--yes: destructive actions default to no": IMPRECISO. mod_05 cleanup ha
     default **y** DELIBERATO (commento nel codice: gli agenti di manutenzione
     devono poter pulire) → `go --yes` PULISCE. Ora la tabella flag dichiara
     l'eccezione esplicitamente (+ `--skip=5` come opt-out); anche la sezione
     del modulo 5 e il callout las la dichiarano.

## Verifica dal vivo (tutti gli esempi README esercitati)
- `0,4,5 --dry-run` → esegue 0,4,5 in ordine; banner "Non-interactive, no
  --yes — every prompt is declined"; nessuna mutazione.
- `go --only=8,9 --dry-run` → solo 8,9. `8,9 --skip=9 --dry-run` → solo 8.
- `--dryrun` → errore + exit 2. `99` → errore, niente eseguito, **exit 0** (bug
  sopra). `make test` → 96/96 (87 selection + 9 guardrails).
- Scheduler NON provato con un agente reale (persistenza launchd invasiva):
  catena verificata = plist template con `<string>${modules}</string>` nel
  codice + gate BM-08c (mock: agente `8,9` esegue solo 8,9).

## Lezione → IMP-002 rafforzata
Il claim exit≠0 era nato asserendo l'rc del FIGLIO (unit test su
`_resolve_cli`) ma mai l'exit END-TO-END del processo che l'utente invoca.
Punto (a) della checklist IMP-002, misurato al bordo reale. Riga aggiunta
all'IMP in [[LEARNINGS]].

## Da decidere (utente)
Micro-fix candidato PRIMA della release: catturare `rc=$?` subito dopo
`script(1)` e propagarlo dopo la strip ANSI (~2 righe in `brew_manager.sh`,
componente sensibile → gate). Senza fix, il README v1.3.0 resta onesto così
com'è ora. → Attenzione #4b.

## Collegamenti
[[STATE]] · [[sessions/2026-07-17-bm08c-agent-selection]] ·
[[sessions/2026-07-14-bm19-readme-reality]] · [[mod-las-scheduler]]
