---
type: session
date: 2026-07-14
status: completed
branch: chore/release-v1.2.0
tags: [session, release, v1.2.0]
---
# Release v1.2.0 — preparazione (tag e push: utente)

## Fatto
- Commit 0dcb072 su `chore/release-v1.2.0`.
- `VERSION` → **1.2.0** (era 1.1.2). Verificato che `--version` legge il nuovo
  valore: `brew-manager 1.2.0 (v1.1.2-44-g3e7b462-dirty)` — il suffisso git
  mostra ancora v1.1.2 perché il tag v1.2.0 non esiste finché non lo crea
  l'utente.
- `CHANGELOG`: `[Unreleased]` svuotata (resta come sezione vuota in cima) e
  contenuto spostato in `## [1.2.0] - 2026-07-14`, RIORGANIZZATO in
  Added / **Changed** / Fixed: i cambi di comportamento che l'utente finale deve
  notare (greedy non aggiorna più tutto; i restore di bk chiedono conferma e in
  `--yes` non fanno nulla; cleanup chiede prima; README che ammette le due
  feature inesistenti) sono in Changed, non nascosti tra i Fixed.
- Prima riga della release: dichiarata come "a safety release", con la sostanza
  in chiaro (dry-run ignorato, greedy fuori scope, adozione mai funzionante).

## Verifica di release
- `make check`: verde su tutti i 21 script.
- `make version-check` sul branch: **ROSSO** (VERSION 1.2.0 vs ultimo tag
  v1.1.2). È il comportamento voluto del guard-rail: segnala che un VERSION
  bumpato attende ancora il suo tag.
- Simulato in un **clone usa-e-getta** il taggging `v1.2.0`: `make version-check`
  → VERDE (`ok VERSION (1.2.0) allineato al tag v1.2.0`). Nessun tag creato nel
  repo reale (verificato: restano solo v1.1.1, v1.1.2, v1.1.2-baseline).

## Confine di esecuzione
Blocco /integrate stampato con i passi espliciti che esegue l'UTENTE: merge,
`git tag -a v1.2.0` (ANNOTATO — i tag storici erano lightweight), push di main e
push del tag. Nessun merge/tag/push eseguito da me.

## Follow-up
- ⛔ **STOP dopo il push di v1.2.0**: M2 (BM-08a, resolver) parte SOLO su via
  libera esplicita dell'utente.
- Dopo il tag, `make version-check` diventerà verde da solo (nessuna azione).
